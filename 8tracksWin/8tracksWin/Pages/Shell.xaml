<Page
    x:Class="_8tracksWin.Pages.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_8tracksWin.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <ControlTemplate x:Key="radioButtonTemplate" TargetType="RadioButton">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{TemplateBinding Tag}" />
                <TextBlock Text="{TemplateBinding Content}" />
            </StackPanel>
        </ControlTemplate>
    </Page.Resources>
    <SplitView Name="shellView" IsPaneOpen="True" DisplayMode="Overlay">
        <SplitView.Pane>
            <StackPanel>
                <RadioButton Tag="&#xe13d;" Content="" Template="{StaticResource radioButtonTemplate}"/>
                <RadioButton Tag="" Content="Liked" Template="{StaticResource radioButtonTemplate}"/>
                <RadioButton Tag="" Content="Listen Later" Template="{StaticResource radioButtonTemplate}"/>
                <RadioButton Tag="" Content="Profile" Template="{StaticResource radioButtonTemplate}"/>
            </StackPanel>
        </SplitView.Pane>
    </SplitView>
</Page>
